# Lesson 01 — C++ Basics & Tooling

Objectives:
- Compile a small program
- Understand header vs implementation

Exercises:
1. Build `src/main.cpp` which uses `math::add`.
2. Modify `include/math/math.h` to add `subtract` and implement it.

Expected outcome: compile and run `app`.
