# Lesson 03 — Libraries & Targets

Objectives:
- Create `mylib` as a library target
- Learn `target_include_directories` and `target_link_libraries`

Exercises:
1. Convert `math` code to a shared library (optional).
2. Add a second consumer demonstrating PUBLIC/PRIVATE includes.
