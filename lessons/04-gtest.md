# Lesson 04 — GoogleTest Basics

Objectives:
- Add unit tests with GoogleTest via CMake `FetchContent`
- Run tests with `ctest`

Exercises:
1. Run `ctest --test-dir build` and inspect failures.
2. Add more test cases to `tests/test_math.cpp`.
