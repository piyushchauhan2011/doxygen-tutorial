# Lesson 02 — CMake Basics

Objectives:
- Learn `cmake` configure/build workflow
- Add simple `CMakeLists.txt` and subdirectories

Exercises:
1. Inspect top-level `CMakeLists.txt` and run `cmake -S . -B build`.
2. Add a new executable in `src/` and wire it in the CMakeLists.
